[user]
# Use primary email and name for github
	name = Hao Chen
	email = hach@microsoft.com

[color]
	ui = auto
	branch = auto
	diff = auto
	status = auto

[alias]
	st = status
# Show log in a compact way
	l = log --graph --oneline --decorate
# Diff file with cached version(Diff after file been added)
	dfc = diff --cached
